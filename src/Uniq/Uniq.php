<?php

namespace App\Uniq;

/**
 * Implement a function `uniq` which takes as input a sequence and returns a sequence
 * in which all duplicate elements following each other have been reduced to one
 * instance.
 *
 * Example:
 * - ['a','a','b','b','c','a','b','c'] --> ['a','b','c','a','b','c']
 *
 * Don't mutate the parameter.
 */
class Uniq
{
    // TODO
}
